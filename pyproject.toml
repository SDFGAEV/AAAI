[project]
name = "cact-agent"
version = "0.1.0"
description = "C-ACT: Contracted Adaptive Counterfactual Trust"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["cact", "xenon_integration"]

