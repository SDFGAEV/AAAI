# NoKnowledge: Pure LLM baseline (DEPS-style)
# No knowledge base. Every decision uses LLM planner only.

cact_method: "NoKnowledge"
frozen: false
active_calib_rate: 0.0
description: "Pure LLM planner, no knowledge base, no memory"
