# FixedBayes: Fixed-threshold Bayesian gate
# Bayesian uplift + harm check with hand-tuned thresholds.
# No adaptive calibration, no contract.

method: "FixedBayes"
frozen: false
active_calib_rate: 0.0
cask_method: "Fixed-Bayes"
description: "Bayesian gate with fixed thresholds (τ=0.90, δ=0.05, h=0.10)"
